import sys

def main():
    data = sys.stdin.read().strip().split()
    N, M = map(int, data[:2])

    k = min((N - 1) // 2, (M - 1) // 2)
    print((N - 2 * k) * (M - 2 * k))

if __name__ == "__main__":
    main()
